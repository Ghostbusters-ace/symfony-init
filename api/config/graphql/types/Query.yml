Query:
  type: object
  config:
    fields:
      Todo:
        type: "Todo"
        args:
          id:
            type: "Int!"
            description: "Get one Todo by its id"
      Todos:
        type: "[Todo]"
